import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#164780"
    },
    secondary: {
      main: "#f2b618"
    },
    text: {
      primary: "#171717"
    }
  }
});

export default theme;
